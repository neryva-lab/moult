---
'@moult/devtools': minor
---

Add `@moult/devtools`: development-time diagnostics for Moult. `timeline()` renders the runtime's transition audit log as a per-plugin chronological generation history; `findLeaks()` surfaces pinned and quarantined generations with retention age, `validate()` issues, plugin error trails, and broken observer listeners. Pure functions over the public runtime inspection surface.
