# Worker host example

This example models a worker host with a message-handler capability. It uses the core runtime and
the public test kit only; its host setup and tests are independent from the command host.
