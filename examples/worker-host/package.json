{
  "name": "@moult/example-worker-host",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . && prettier --check .",
    "test": "vitest run --config vitest.config.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@moult/runtime": "workspace:*",
    "@moult/test": "workspace:*"
  }
}
