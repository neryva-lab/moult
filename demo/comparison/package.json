{
  "name": "@moult/comparison-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . && prettier --check .",
    "build": "tsc",
    "bench": "tsc && node dist/bench.js && prettier --write RESULTS.md benchmark.json"
  },
  "dependencies": {
    "@moult/runtime": "workspace:*",
    "@moult/test": "workspace:*",
    "cordis": "4.0.0-rc.9",
    "mitata": "1.0.34"
  }
}
